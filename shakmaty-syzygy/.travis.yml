language: rust
sudo: false

rust:
 - 1.22.0
 - 1.23.0
 - 1.24.0
 - stable
 - beta
 - nightly

cache:
  directories:
   - tables

before_install:
 - ./download-test-tables.sh

before_cache:
 - rm -f tables/**/*.txt
